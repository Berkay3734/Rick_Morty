import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    width: 50,
    height: 50,
    position: 'absolute',
    top: 20,
    left: 20,
    zIndex: 1,
  },
});
